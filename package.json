{
  "name": "school-website",
  "version": "1.0.0",
  "description": "School Website - Full Stack (React + Node + MySQL)",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "start:backend": "cd backend && npm run dev",
    "start:frontend": "cd frontend && npm start",
    "start": "start cmd /k \"cd backend && npm run dev\" && start cmd /k \"cd frontend && npm start\""
  },
  "devDependencies": {}
}
